We have a suite of tests:

- deck config tests using zod (`./src/decks/deck-config.spec.ts`)
- deck spawner tests (`./src/state/__tests__/deck-spawner.spec.ts`)
- state machine tests (`./src/state/__tests__/*`)
