import Tile from "@/components/Tile";
import withMaterialProvider from "@/components/utils/withMaterialProvider";

const TileWithProvider = withMaterialProvider(Tile);

export default TileWithProvider;
