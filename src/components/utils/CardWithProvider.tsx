import Card from "@/components/Card";
import withMaterialProvider from "@/components/utils/withMaterialProvider";

const CardWithProvider = withMaterialProvider(Card);

export default CardWithProvider;
